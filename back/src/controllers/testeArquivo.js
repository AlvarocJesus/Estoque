const fs = require('fs');

const data = 'name,preco,quantidade,precoUnitario,fornecedor,precoCusto';

/* class TesteArquivo{
  async function teste() {
    fs.writeFile('teste.csv', data, (err) => {
      if (err) throw err;

      console.log('deu certo!');
    });
  }
} */